import React, { FC } from 'react'
import { CreateUserHeight } from './CreateUserHeight'

const CreateUserHeightMeta = {
    title: 'Pages/Auth/CreateUserHeight',
    component: CreateUserHeight,
    decorators: [(Story: FC) => <Story />],
}

export default CreateUserHeightMeta

export const Basic = {}
