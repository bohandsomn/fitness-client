import { ReactElement } from 'react'

export interface ILogInProps {
    emailField: ReactElement
    passwordField: ReactElement
    logInSubmitButton: ReactElement
    createAccountLink: ReactElement
}