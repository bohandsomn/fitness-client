import React, { FC } from 'react'
import { ReloadIcon } from './ReloadIcon'

const ReloadIconMeta = {
    title: 'Shared/ReloadIcon',
    component: ReloadIcon,
    decorators: [(Story: FC) => <Story />],
}

export default ReloadIconMeta

export const Basic = {}
